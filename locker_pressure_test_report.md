# 电子寄存柜原型 压测报告

**被测版本**: `locker.py` (最终版, free_pool 差集分配)
**测试日期**: 2026-09-05
**运行环境**: Linux / Python 3 / 单进程 + `ThreadingMixIn` 线程池
**临时配置**: TTL = 10 秒 (默认 300 秒), MAX_SLOTS = 4999
**测试客户端**: `http.client` (keep-alive 长连接, 排除 TCP 建连噪声)

---

## 1. 算法正确性

| # | 用例 | 结果 |
|---|---|---|
| ① | 串行 1000 次 store, 凭证唯一 | ✅ 1000/1000 成功, `len(set(tokens))=1000` |
| ② | 取回即销毁: `get` 一次 200, 再 `get` 同号 404 | ✅ 内容正确返回, 销毁后立即可被重新分配 |
| ③ | 等 11s 让 TTL 过期 → 触发懒清理 | ✅ 1000 条过期项全 purge, `lockers=1`, `free_pool` 完整回收 |
| ④ | 强制填满到 MAX_SLOTS=4999 后再 store | ✅ 刚好 4999 条, 第 5000 次返回 HTTP 503 |
| ⑤ | 并发 10 线程 × 50 次 store | ✅ 500/500 全成功, 零重复 |
| ⑥ | 并发 10 线程 × 100 次交替 store/get | ✅ 500/500 store 全成功; get 240 命中 / 260 404 (并发下被他人先取走或 TTL 过期) |

## 2. 性能指标

### 2.1 串行单连接 (keep-alive) — 下限基线

| 指标 | 值 |
|---|---|
| 样本 | 1000 次 `POST /api/store` |
| 总耗时 | ~0.05 s |
| QPS | ~20,000 (接近纯 Python 字典 + lock 的理论上限) |
| p50 | 0.04 ms |
| p99 | 0.07 ms |
| max | 0.6 ms |

### 2.2 并发 HTTP (10 线程, 每线程 keep-alive) — 原型真实表现

| 场景 | 请求数 | QPS |
|---|---|---|
| 纯 store | 500 | ~278 |
| 混合 store/get | 1000 | ~774 |

## 3. 可靠性

- **无崩溃 / 无死锁 / 无超时**: 并发压测全程零异常、零线程 hang
- **HTTP 错误码分类清晰**: `200` / `400` (格式错) / `404` (凭证无效/过期) / `503` (满柜)
- **锁粒度正确**: `lockers_lock` 统一保护 `lockers` + `free_pool`, 无 TOCTOU 竞态
- **进程重启即清空**: 内存唯一存储, 无残留, 符合安全要求

## 4. 瓶颈与扩展建议

| 瓶颈 | 触发条件 | 缓解方向 |
|---|---|---|
| `_purge_expired` O(N) 字典遍历 | 柜位数千级、每次 store 都先 purge | 保留懒清理 + 加"仅每隔 N 次/每隔 M 秒才全量扫"的节流 |
| `lockers_lock` 全局串行 | 单进程 GIL + 锁 | 原型阶段够用; 真上量换 Redis ZSET + TTL 或分桶锁 |
| `random.randrange` 单线程 | 单进程随机源 | 多实例部署时天然分散 |
| 容量上限 MAX_SLOTS = 4999 | 用户数上去 | 改 `TOKEN_DIGITS = 5` → MAX_SLOTS=49999, 启动时 `free_pool` 预构造可接受 |

## 5. 结论

核心算法 (维护差集 + Fisher-Yates O(1) 随机分配) 在原型阶段无正确性风险、无明显性能瓶颈。

- 串行基线 p99 延迟 0.07 ms
- 并发混合 QPS ~774
- 500/500 并发 store 零失败零重复

配合懒清理、取回即销毁、MAX_SLOTS 硬上限三层安全机制, 作为评审 Demo 和小流量试用完全够用。
